[package]
name = "lorica-common"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]

# Test-only, and declared here rather than in the workspace: nothing else in the
# tree uses them, and the crate compiled into the kernel program has no dependency.
[dev-dependencies]
criterion = "0.7"
proptest = "1.8"

[[bench]]
name = "bucket"
harness = false
